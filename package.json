{
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "find . -type d -name 'dist' -prune -exec rm -rf {} + && find . -type d -name 'generated' -prune -exec rm -rf {} + && find . -name '*.db' -delete && find . -type d -name 'node_modules' -prune -exec rm -rf {} +"
  }
}
